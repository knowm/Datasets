KEY_CREATE_TABLE=CREATE TABLE BIRD_SONGS (bagid INTEGER NOT NULL, labels VARCHAR(256) NOT NULL, wavfilename VARCHAR(256) NOT NULL, wavbytes blob NOT NULL, PRIMARY KEY (bagid))
KEY_CREATE_TABLE_TENFOLD=CREATE TABLE TEN_FOLD (bagid INTEGER NOT NULL, fold INTEGER NOT NULL, PRIMARY KEY (bagid))