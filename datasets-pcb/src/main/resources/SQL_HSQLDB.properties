KEY_CREATE_TABLE=CREATE CACHED TABLE PCB (id INTEGER NOT NULL, imgbytes blob NOT NULL, PRIMARY KEY (id))
KEY_CREATE_TABLE_PCB_ANNOTATIONS=CREATE CACHED TABLE PCB_ANNOTATIONS (pcbid INTEGER NOT NULL, id INTEGER NOT NULL, x FLOAT NOT NULL, y FLOAT NOT NULL, width FLOAT NOT NULL, height FLOAT NOT NULL, rotation FLOAT NOT NULL, name VARCHAR(1000) NULL, CONSTRAINT PRI PRIMARY KEY ( pcbid, id ))