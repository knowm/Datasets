KEY_CREATE_TABLE=CREATE TABLE PCB (id INTEGER NOT NULL, imgbytes mediumblob NOT NULL, PRIMARY KEY (id))
KEY_CREATE_TABLE_PCB_ANNOTATIONS=CREATE TABLE PCB_ANNOTATIONS (pcbid INTEGER NOT NULL, id INTEGER NOT NULL, x FLOAT NOT NULL, y FLOAT NOT NULL, width FLOAT NOT NULL, height FLOAT NOT NULL, rotation FLOAT NOT NULL, name VARCHAR(1000) NULL, PRIMARY KEY (`pcbid`, `id`))