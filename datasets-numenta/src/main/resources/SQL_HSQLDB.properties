KEY_CREATE_TABLE=CREATE CACHED TABLE NUMENTA (id BIGINT NOT NULL, seriesGroup VARCHAR(500) NOT NULL, seriesName VARCHAR(500) NOT NULL, timestamp BIGINT NOT NULL, value DOUBLE NOT NULL, label TINYINT NOT NULL, PRIMARY KEY (id))